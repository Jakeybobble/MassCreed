x, y = 0, 0
rotation = 0
depth = 0

function class:init(x, y, rot, depth)
    if x ~= nil then self.x = x end
    if y ~= nil then self.y = y end
    if rot ~= nil then self.rot = rot end
    if depth ~= nil then self.depth = depth end
    

end

function class:update(dt)

end

function class:draw()

end